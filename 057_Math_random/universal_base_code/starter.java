import pkg.*;
import java.util.Scanner;
import java.util.Random;


class starter {
	public static void main(String args[]) {
		
		Math.floor(Math.random()*10);
		double one = Math.floor(Math.random()*100)+1;
		double two = Math.floor(Math.random()*1)+2.5;
		double num = Math.floor(Math.random()*575)+14;
		System.out.println(num);
		
	}
}
